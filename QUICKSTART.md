# Быстрый старт QUIK Module

## Минимальная установка (5 минут)

### 1. Установка зависимостей
```powershell
npm install
```

### 2. Создание .env файла
Создайте файл `.env` со следующим содержимым:
```env
PORT=5000
SERVER_HOST=127.0.0.1
NODE_ENV=production
LOG_LEVEL=info
```

### 3. Настройка Firewall (от имени администратора)
```powershell
.\scripts\setup-firewall.ps1
```

### 4. Установка PM2
```powershell
npm install -g pm2
```

### 5. Запуск как служба
```powershell
.\scripts\install-service.ps1
```

## Проверка работы

```powershell
# Проверка статуса
pm2 status

# Проверка health endpoint
curl http://localhost:5000/health

# Проверка безопасности
.\scripts\check-security.ps1
```

## Основные команды

```powershell
# Запуск
npm run pm2:start

# Остановка
npm run pm2:stop

# Перезапуск
npm run pm2:restart

# Логи
npm run pm2:logs

# Тесты
npm test
```

## Важные замечания

⚠️ **КРИТИЧЕСКИ ВАЖНО:**
- `SERVER_HOST` НЕ должен быть `0.0.0.0`
- Используйте конкретный IP локальной сети (например, `192.168.1.10`) или `127.0.0.1`
- Настройте Firewall для блокировки внешних подключений

📖 Подробная документация: см. `README.md` и `DEPLOYMENT.md`

