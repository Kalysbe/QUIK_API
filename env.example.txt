# Server Configuration
PORT=5000
SERVER_HOST=0.0.0.0
# 0.0.0.0 — слушать на всех сетевых интерфейсах
# Для доступа только локально используйте 127.0.0.1 или конкретный IP (например, 192.168.1.10)

# Allowed Networks (разрешенные сети для доступа)
# * или 0.0.0.0/0 — доступ с любых IP
# Для ограничения: CIDR через запятую, например 192.168.0.0/16,10.0.0.0/8
ALLOWED_NETWORKS=*

# Environment
NODE_ENV=production

# Logging
LOG_LEVEL=info
# Возможные значения: error, warn, info, verbose, debug, silly

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database
DB_USER=your_user
DB_PASSWORD=your_password

# Database Configuration (MSSQL)
MSSQL_SERVER=localhost
MSSQL_DATABASE=your_database
MSSQL_USER=your_user
MSSQL_PASSWORD=your_password
MSSQL_OPTIONS_TRUSTED_CONNECTION=false

